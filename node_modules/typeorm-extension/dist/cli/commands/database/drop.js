"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DatabaseDropCommand = void 0;
const data_source_1 = require("../../../data-source");
const database_1 = require("../../../database");
class DatabaseDropCommand {
    constructor() {
        this.command = 'db:drop';
        this.describe = 'Drop database.';
    }
    builder(args) {
        return args
            .option('root', {
            alias: 'r',
            default: process.cwd(),
            describe: 'Path to the data-source / config file.',
        })
            .option('connection', {
            alias: 'c',
            default: 'default',
            describe: 'Name of the connection on which run a query.',
            deprecated: true,
        })
            .option('config', {
            alias: 'f',
            default: 'ormconfig',
            describe: 'Name of the file with the data-source configuration.',
            deprecated: true,
        })
            .option('dataSource', {
            alias: 'd',
            default: 'data-source',
            describe: 'Name of the file with the data-source.',
        })
            .option('initialDatabase', {
            describe: 'Specify the initial database to connect to.',
        });
    }
    async handler(raw, exitProcess = true) {
        const args = raw;
        const dataSourceOptions = await (0, data_source_1.buildDataSourceOptions)({
            name: args.connection,
            configName: args.config,
            directory: args.root,
            dataSourceName: args.dataSource,
        });
        const context = {
            ifExist: true,
            options: dataSourceOptions,
        };
        if (typeof args.initialDatabase === 'string' &&
            args.initialDatabase !== '') {
            context.initialDatabase = args.initialDatabase;
        }
        await (0, database_1.dropDatabase)(context);
        if (exitProcess) {
            process.exit(0);
        }
    }
}
exports.DatabaseDropCommand = DatabaseDropCommand;
